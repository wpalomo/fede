'use strict';

describe('fedeApp.view2 module', function() {

  beforeEach(module('fedeApp.view2'));

  describe('fedeApp controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});